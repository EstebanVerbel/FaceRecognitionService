package constants

// KeyspaceName is the name of Cassandra's database
const KeyspaceName = "face_verification"

// ClusterAddress holds the address of the cluster where is hosted the database (local host for now)
const ClusterAddress = "127.0.0.1"
